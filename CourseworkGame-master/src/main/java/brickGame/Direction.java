package brickGame;

/**
 * Enum representing the possible directions for movement in the brick game.
 * This enum is used to define the direction in which certain game elements, like the paddle, can move.
 */
public enum Direction {
    LEFT, RIGHT
}
